<% unless current_user?(@user) %>
  <%= turbo_frame_tag "follow_form_#{@user.id}" do %>
    <div id="follow_form">
      <% if current_user.following?(@user) %>
        <%= render "unfollow" %>
      <% else %>
        <%= render "follow" %>
      <% end %>
    </div>
  <% end %>
<% end %>
