<%= form_with(
      model: current_user.active_relationships.find_by(followed_id: @user.id),
      method: :delete,
      remote: true
    ) do |f| %>
  <%= f.submit t("users.follow.unfollow_button"), class: "btn btn-secondary" %>
<% end %>
