<h1><%= t("products.titles.editing") %></h1>

<%= render "form", product: @product %>

<br>

<div>
  <%= link_to t("products.links.show"), @product %> |
  <%= link_to t("products.links.back"), products_path %>
</div>
